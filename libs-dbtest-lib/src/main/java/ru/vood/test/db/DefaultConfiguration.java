package ru.vood.test.db;

import freemarker.template.Version;

public class DefaultConfiguration extends freemarker.template.Configuration {
    public DefaultConfiguration(Version incompatibleImprovements) {
        super(incompatibleImprovements);
    }
}
